/**
 * Наброски для модуля работы с Kit Online
 */

/*if (env.payment.cashboxMode === CASHBOX_MODE.KIT_ONLINE) {
  const authSettings = env.payment.online.settings.auth;
  const dataSettings = env.payment.online.settings.data;
  storage.kitOnlineInstance = new KitOnline(authSettings.companyId, authSettings.userLogin, authSettings.password,
    dataSettings.getFiscalData, dataSettings.getCheckLink, dataSettings.getQRCode);
}*/
